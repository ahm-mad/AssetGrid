{
  "name": "emax-etl",
  "private": true,
  "type": "module",
  "description": "MySQL + MongoDB -> Supabase Postgres ETL. See docs/target/data-migration.md.",
  "scripts": {
    "etl": "node etl.ts",
    "verify": "node verify.ts",
    "typecheck": "tsc -p tsconfig.json"
  }
}
